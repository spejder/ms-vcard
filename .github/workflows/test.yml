permissions:
  contents: read
name: Build and test
on:
  pull_request:
  push:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Docker build
        run: |
          docker build .
